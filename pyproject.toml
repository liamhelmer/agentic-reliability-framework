[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-reliability-framework"
version = "3.3.0"
description = "Production-grade multi-agent AI system for infrastructure reliability monitoring and self-healing - OSS Edition"
readme = "README.md"
requires-python = ">=3.10"  # 🔄 FIXED: Back to 3.10
license = {text = "Apache 2.0"}
authors = [
    {name = "Juan Petter", email = "petter2025us@outlook.com"}
]

# ... rest of file ...

[tool.black]
line-length = 100
target-version = ['py310']  # 🔄 FIXED: Back to py310

[tool.ruff]
line-length = 100
target-version = "py310"  # 🔄 FIXED: Back to py310

[tool.mypy]
python_version = "3.10"  # 🔄 FIXED: Back to 3.10
